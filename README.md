## Prerequisites
- Java JDK 21 (or whatever version you're using)

## Running
./gradlew run       # Mac/Linux
gradlew.bat run     # Windows

## Branch Rules
- Never push directly to main
- Create a feature branch then open a PR to merge
